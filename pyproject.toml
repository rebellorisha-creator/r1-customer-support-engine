[project]
name = "customer-support-engine"
version = "1.0.0"
description = "RL-based customer support triage system"
authors = [{name = "anonymous"}]

dependencies = [
    "openenv-core>=0.2.0"
]

[project.scripts]
server = "app:app"